# Используем образ Node.js на базе Alpine для меньшего размера
FROM node:lts-alpine

# Устанавливаем рабочую директорию
WORKDIR /front

# Копируем только файлы зависимостей для оптимизации кэширования
COPY package*.json ./

# Устанавливаем зависимости
RUN npm install

# Копируем остальные файлы приложения в контейнер
COPY . .

# Устанавливаем дополнительные зависимости, если они нужны
RUN npm install react-responsive

# Строим приложение
RUN npm run build

# Открываем порт для приложения
EXPOSE 3000

# Запускаем приложение в режиме разработки
CMD ["npm", "run", "dev"]
